import '../atom/atom.l10n.base.dart';
import '../prefix/prefix.l10n.base.dart';

abstract class Units implements AtomV1Localization, PrefixV1Localization {
  const Units();
}

class UnitsEn extends Units {
  const UnitsEn();
}
